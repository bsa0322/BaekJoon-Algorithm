#include <iostream>

using namespace std;

int table[51][3];

int main() {
    int a, b, c;

    while (true) {
        cin >> a >> b >> c;
        if (a == -1 && b == -1 && c == -1)
            break;

        for(int i=a;i)

    }


    return 0;
}